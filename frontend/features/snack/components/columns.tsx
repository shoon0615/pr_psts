/* 'use client'

import { ColumnDef } from '@tanstack/react-table'

export type Board = {
  id: number
  title: string
  status: string
}

export const columns: ColumnDef<Board>[] = [
  {
    accessorKey: 'id',
    header: '번호'
  },

  {
    accessorKey: 'title',
    header: '제목'
  },

  {
    accessorKey: 'status',
    header: '상태'
  }
] */
